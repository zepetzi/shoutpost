import { useRef, useState } from 'react';
import { Image } from 'react-konva';

export default function Post({imageURL, thumbURL}) {
    return (
        <>
        <Image></Image>
        </>
    )
}
